import 'package:flutter_test/flutter_test.dart';
import 'package:fp_recipe_book/recipe.dart';

void main() {
  test(
      'I can retrieve the entire list of Ingredient Objects based off of the Json information',
      () {
    Recipe testRecipe = Recipe();
  });
}
